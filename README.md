grade book analyzer

This grade book analyzer is made using in python in VS code .

In the output  if entering name is done and entered the marks to end the code press enter in the blank space of name without entering the name.
